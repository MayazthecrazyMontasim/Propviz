# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.50
asyncpg==0.31.0
alembic==1.14.0

# Config
pydantic-settings==2.6.1

# Task queue
celery==5.4.0
redis==5.2.0

# Storage
boto3==1.35.68

# Anthropic (Claude)
anthropic==0.40.0

# Google Gemini (free-tier alternative)
google-generativeai==0.8.3

# Groq (free-tier, fast inference, vision support)
groq==0.11.0

# Free TTS — Microsoft Edge neural voices, no API key needed
edge-tts==7.0.2

# HTTP client (0.28+ removed 'proxies' arg that groq SDK uses)
httpx==0.27.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
email-validator==2.3.0
psycopg2-binary==2.9.10

# Audio / Video (ffmpeg is installed via system package)
# ffmpeg-python is a thin wrapper — actual encoding handled by ffmpeg binary
ffmpeg-python==0.2.0
